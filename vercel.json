{
  "version": 2,
  "buildCommand": "vite build",
  "outputDirectory": "dist/client",
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/(.*)",
      "dest": "/api/server"
    }
  ],
  "functions": {
    "api/server.js": {
      "includeFiles": "dist/server/**"
    }
  }
}
